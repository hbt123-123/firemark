# Embedding API 多提供商配置示例
# 
# 格式说明:
# - url: API 端点 URL
# - key: API Key
# - model: 使用的模型
# - name: (可选) 提供商名称，用于日志
#
# 使用方式:
# 1. 将下方配置复制到 .env 文件中
# 2. 或设置 EMBEDDING_PROVIDERS 环境变量（JSON 格式）
#
# 注意事项:
# - 当前一个 API 失败（RateLimit/Timeout/Error）时，自动切换到下一个
# - 会依次尝试所有配置的提供商，直到成功或全部失败
# - 如果不设置 EMBEDDING_PROVIDERS，会回退到旧的单 API 配置

# ============================================
# 方式一：多个 API 配置（推荐）
# ============================================
# JSON 字符串，需要转义引号
EMBEDDING_PROVIDERS=[{"url":"https://api.openai.com/v1","key":"sk-your-openai-key","model":"text-embedding-3-small","name":"openai"},{"url":"https://api.siliconflow.cn/v1","key":"sk-your-silicon-key","model":"BAAI/bge-large-zh-v1.5","name":"siliconflow"},{"url":"http://localhost:11434/v1","key":"ollama","model":"nomic-embed-text","name":"ollama-local"}]

# ============================================
# 方式二：单个 API 配置（兼容旧版）
# ============================================
# EMBEDDING_API_KEY=sk-your-api-key
# EMBEDDING_API_BASE_URL=https://api.openai.com/v1
# EMBEDDING_MODEL=text-embedding-3-small
